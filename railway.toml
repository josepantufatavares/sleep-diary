[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm rebuild better-sqlite3"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
